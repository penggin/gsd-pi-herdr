[workspace]
members = ["crates/*"]
resolver = "2"

[workspace.package]
version = "1.16.2"
edition = "2021"
license = "MIT"
authors = ["GSD Contributors"]
repository = "https://github.com/penggin/gsd-pi-herdr"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = true
panic = "abort"

[profile.dev]
codegen-units = 256
incremental = true
